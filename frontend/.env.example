# Backend API base URL (no trailing slash).
# Unset → defaults to http://localhost:8080 (local dev).
# For a deployed backend, copy to `.env.local` and set the public URL.
VITE_API_BASE=http://localhost:8080
